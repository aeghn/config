#!/usr/bin/env bash

set -eu

# Created by dfs

# DFS_DIR := work dir
# LINK_MODE := CP | LN | DEF
# GIT_MANAGED := false | true
#   GIT_DIRTY := false | true

# File to store and restore.

store() {
    warn "store not implement, $PWD"
}

restore() {
	for i in *; do
		kmap "$DFS_DIR/$i" "$HOME/.config/fcitx5/$i"
	done	
}
