#!/usr/bin/env bash

set -eu

# Created by dfs

# WORK_DIR := work dir
# LINK_MODE := CP | LN | DEF
# GIT_MANAGED := false | true
#   GIT_DIRTY := false | true

# File to store and restore.

store() {
    warn "store not implement, $PWD"
}

restore() {
	set -x
	for i in *; do
		ln -s "$WORK_DIR/$i" "$HOME/.config/fcitx5/$i"
	done	
}
