#!/usr/bin/env bash

set -eu

# Created by dfs

# WORK_DIR := work dir
# LINK_MODE := CP | LN | DEF
# GIT_MANAGED := false | true
#   GIT_DIRTY := false | true

# File to store and restore.

store() {
    warn "store not implement, $PWD"
}

restore() {
    bak-mv "$HOME/.config/rgui"
    ln -sf "$WORK_DIR" "$HOME/.config/rgui"
}
